<!--
This file was generated by Spark. Do not edit it by hand.
-->
# AshExclusiveArc.Resource

An Ash resource extension implementing the exclusive belongs-to (exclusive arc) pattern.

This provides referential-integrity-safe polymorphic relationships using multiple
nullable foreign keys with a CHECK constraint ensuring exactly one is non-null.

## Usage

    defmodule MyApp.CartItem do
      use Ash.Resource,
        extensions: [AshExclusiveArc.Resource]

      exclusive_arc do
        arc :purchasable do
          belongs_to :product_variant, MyApp.ProductVariant
          belongs_to :subscription_plan, MyApp.SubscriptionPlan
        end

        arc :owner do
          belongs_to :customer, MyApp.Customer
          belongs_to :guest_session, MyApp.GuestSession
        end
      end
    end

## Referential Integrity

By default, the extension generates database-level constraints (CHECK constraint,
foreign keys, partial unique indexes). This can be opted out per-arc or globally:

    exclusive_arc do
      arc :owner, referential_integrity: false do
        belongs_to :customer, MyApp.Customer
      end
    end

## Soft-Delete (AshArchival) Support

When the source resource uses `ash_archival`, partial unique indexes automatically
exclude archived records. This is detected automatically but can be configured:

    exclusive_arc do
      arc :purchasable, archive_column: :archived_at do
        belongs_to :product_variant, MyApp.ProductVariant
      end
    end


## exclusive_arc
Configures exclusive arc (exclusive belongs-to) relationships on this resource.
Each arc defines a group of mutually exclusive belongs_to relationships where
exactly one must be non-null.


### Nested DSLs
 * [arc](#exclusive_arc-arc)
   * belongs_to





### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`referential_integrity`](#exclusive_arc-referential_integrity){: #exclusive_arc-referential_integrity } | `boolean` | `true` | Default referential integrity setting for all arcs in this section. When true, generates CHECK constraints, foreign keys, and partial unique indexes. When false, only Ash-layer validation is used. |



### exclusive_arc.arc
```elixir
arc name
```


Defines an exclusive arc — a group of mutually exclusive belongs_to relationships
where exactly one must be set at any time.


### Nested DSLs
 * [belongs_to](#exclusive_arc-arc-belongs_to)




### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#exclusive_arc-arc-name){: #exclusive_arc-arc-name .spark-required} | `atom` |  | The name of this exclusive arc (e.g., :purchasable, :owner). |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`referential_integrity`](#exclusive_arc-arc-referential_integrity){: #exclusive_arc-arc-referential_integrity } | `boolean` |  | Whether to generate database-level constraints (CHECK constraint, foreign keys, partial unique indexes). Defaults to the section-level setting. Set to `false` for cross-database references, resources without AshPostgres, or when you want Ash-layer validation only. |
| [`archive_aware`](#exclusive_arc-arc-archive_aware){: #exclusive_arc-arc-archive_aware } | `boolean` | `true` | Whether partial unique indexes should exclude archived records when the source resource uses `ash_archival`. Detected automatically when true. |
| [`archive_column`](#exclusive_arc-arc-archive_column){: #exclusive_arc-arc-archive_column } | `atom` |  | Override the archive column name for partial index conditions. Detected automatically from `ash_archival` config when nil. |


### exclusive_arc.arc.belongs_to
```elixir
belongs_to name, destination
```


Declares one possible target for this exclusive arc.





### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#exclusive_arc-arc-belongs_to-name){: #exclusive_arc-arc-belongs_to-name .spark-required} | `atom` |  | The name of this reference (used as the relationship name). |
| [`destination`](#exclusive_arc-arc-belongs_to-destination){: #exclusive_arc-arc-belongs_to-destination .spark-required} | `atom` |  | The destination resource module. |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`attribute_type`](#exclusive_arc-arc-belongs_to-attribute_type){: #exclusive_arc-arc-belongs_to-attribute_type } | `any` | `:uuid` | The type of the foreign key attribute. |
| [`define_attribute`](#exclusive_arc-arc-belongs_to-define_attribute){: #exclusive_arc-arc-belongs_to-define_attribute } | `boolean` | `true` | Whether to define the foreign key attribute. Set to false if you define it yourself. |














<style type="text/css">.spark-required::after { content: "*"; color: red !important; }</style>
